spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dbspotmusic?useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    properties:
      hibernate:
        show_sql: true
        hbm2ddl:
          auto: update


#          #show_sql: true  os comandos devem ser executados no console
#          #auto: update indica que as tabelas devem ser atualzidas a cada compilação caso haja mudanças




